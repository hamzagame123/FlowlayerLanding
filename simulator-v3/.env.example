# https://ion.cesium.com/tokens — create a token with assets:read (Cesium OSM Buildings is asset 96188 by default)
VITE_CESIUM_ION_TOKEN=

# Optional: override ion asset id if your account uses a different OSM Buildings asset
# VITE_CESIUM_ION_OSM_ASSET_ID=96188
